#include <stdio.h>

int main()
{

    int length = 100;
    int width = 200;

    printf("The Length is %d.\n", length);
    printf("The Width is %d.\n", width);
    printf("The Area is %d.\n", length * width);
}